using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class UIcontroller : MonoBehaviour
{
    /*
    TMP_Text correctAns;
    BodyContact contact;
    //public int ans = 0;
    // Start is called before the first frame update
    void Awake()
    {
        contact = GameObject.Find("TestContact (2)").GetComponent<BodyContact>();
        correctAns = GameObject.Find("correctAns").GetComponent<TMP_Text>();
    }

    // Update is called once per frame
    void Update()
    {   
        if (contact.heartbeats?[0])
        {
            int ?ans = 60;
            correctAns.text = "~" + ans + " BPM";
        }
        
    }
    */
}
